# 오디세이 런 — P0 런타임 에셋 인벤토리

> P0 = 키클롭스 섬 vertical slice (Intro Story → Ready → Island Stage → 장애물 → Stage Story → Finish → Result).
> 원본은 **repo 밖**(`~/Documents/playzera-assets/오디세이/`)에 두고, 최적화 출력만 커밋한다.
> 파이프라인: `tools/glb/optimize.mjs`(GLB) · `tools/img/to-webp.mjs`(이미지).
> 소스 매핑 정본: `tools/glb/odyssey-p0.sources.json` · `tools/img/odyssey-p0.story.json`.

## 신규 GLB (3)

| 원본 (repo 밖) | 런타임 파일 | 용량 | 삼각형 | 텍스처 | 용도 |
|---|---|---:|---:|---|---|
| `glb/ornate gate 3d model.glb` (54.7MB) | `runner3d/odyssey/island/obstacles/gate_finish.glb` | **2.42MB** | 1.94M → 77k | 2K×3 → 1K×1 JPEG | 섬 Finish 관문 (`portal.js` 지오 교체) |
| `glb/yoga pose sign 3d model.glb` (27.7MB) | `runner3d/odyssey/island/obstacles/sign_pose.glb` | **1.09MB** | 963k → 29k | 2K×3 → 1K×1 JPEG | 자세 팻말 (poseMatch, `pose_holds`) |
| `glb/gladiator+warrior+3d+model.glb` (29.6MB) | `runner3d/odyssey/island/props/statue_warrior.glb` | **2.02MB** | 946k → 60k | 2K×3 → 1K×1 JPEG | 좌우 배경 랜드마크 (`PropRow`) — **선택** |

- 3파일 합계: 111.9MB → **5.53MB** (−95.1%)
- 전부 메시 1개 / 재질 baseColor만 / 확장 0 / `MeshBasicMaterial` 계약(`glbProp.js`)에 맞음
- three `GLTFLoader.parse` 로드 확인 (지오메트리·UV·임베드 JPEG 정상)

## 신규 스토리 이미지 (5)

| 원본 (repo 밖) | 런타임 파일 | 용량 | 해상도 | 배치 |
|---|---|---:|---|---|
| `story/final/scene01-01.png` (2.81MB) | `runner3d/odyssey/story/story_intro_1.webp` | 207KB | 1600×900 | 인트로 #1 (도움 청함) |
| `story/final/scene01-02.png` (2.79MB) | `.../story_intro_2.webp` | 204KB | 1600×900 | 인트로 #2 (수락·출항) |
| `story/final/scene02-01.png` (2.76MB) | `.../story_intro_3.webp` | 199KB | 1600×900 | 인트로 #3 (섬 도착·외눈) |
| `story/final/scene02-03.png` (3.17MB) | `.../story_island_1.webp` | 268KB | 1600×900 | 섬 스테이지 스토리 |
| `story/final/scene02-04.png` (3.04MB) | `.../story_island_finish.webp` | 247KB | 1600×900 | Finish 비트 (탈출) |

- 5파일 합계: 14.56MB → **1.10MB** (−92.4%), WebP q80
- 소년 스킨만 존재 — **소녀 버전 필요 여부 ken 확인** (쥬라기는 양쪽 다 있음)

## 재사용 (이번 단계에서 복사 안 함)

lab-island이 이미 쓰는 `_lab/` GLB — 게임 통합 단계에서 `odyssey/island/`로 이전 + 재최적화:
- `_lab/ship_odyssey.glb` (1.9MB) — 배경 범선
- `_lab/temple_odyssey.glb` (1.3MB) — 배경 "섬" 랜드마크
- `_lab/godstatue_odyssey.glb` (3.9MB, 포세이돈) — **재최적화 권장** (1K, 더 깎기 → ~1.5MB)
- `_lab/island/giant_odyssey.glb` (665KB) — 레인 장애물
- 바닥(`ground.js` 대리석 팔레트) · 물(`water.js`) · 하늘(쥬라기 `bg_sky.png` 재사용) — GLB 아님

## P0 런타임 총량 (추정)

| | 크기 |
|---|---:|
| 신규 GLB (gate + sign, warrior 제외) | 3.5MB |
| 신규 GLB (warrior 포함) | 5.5MB |
| 재사용 GLB (ship + temple + godstatue재최적화 + giant) | ~4.7MB → 재최적화 시 ~3.5MB |
| 스토리 5장 | 1.1MB |
| 점프/숙이기 장애물 (쥬라기 지오 재사용 or primitive) | ~0.5MB |
| 하늘 (쥬라기 재사용 시 0, 신규 시 ~0.4MB) | 0~0.4MB |
| **섬 슬라이스 콜드 다운로드 (엔진·three 제외)** | **~8.5MB (warrior 포함), ~6.5MB (제외)** |
| + BGM (스트리밍) | ~1.5MB |

## 연결 상태 (STEP 78)

전부 `src/games/odyssey-run/`에 연결됨:
- `gate_finish.glb` → 섬 스테이지 Finish 관문(시각 랜드마크, `scene.js`)
- `sign_pose.glb` → 자세 팻말(3자세 공용, `scene.js`)
- `statue_warrior.glb` → 배경 전사상(`USE_WARRIOR` 플래그, 기본 켬)
- 스토리 5장 → `manifest.json`의 `story.intro`(3) + `story.finish`(2)
- 재사용: `_lab/ship_odyssey.glb`·`_lab/temple_odyssey.glb`·`_lab/island/giant_odyssey.glb`

## 향후

- `godstatue_odyssey` 재최적화 (3.9MB는 배경 랜드마크치고 무겁다)
- `_lab/` → `odyssey/`·`_shared/` 이전 후 `_lab/` 삭제 (지금 ~20MB가 프로덕션 `dist/`에 실림)
- `tools/check.mjs` 예산: `public/assets/runner3d/odyssey` 항목 신설 (현재 `runner3d` 7MB 상한이 `_lab` 때문에 이미 초과)
- P1(바다)·P2(이타카) GLB는 `tools/glb/*.sources.json` 추가로
