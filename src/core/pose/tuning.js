// 포즈·제스처 튜닝값 — 게임 공용 정본.
//
// 이 숫자들은 현장 테스트로 다듬어진 것이다. **구조를 바꿔도 값은 건드리지 않는다.**
// 주석에 적힌 이유가 곧 바꾸면 안 되는 근거다.
//
// 원래 `warmup-obstacle/config.js`에 있었고 웜업에서만 쓰였다. 똥 피하기에도 O/X를
// 붙이면서 여기로 올렸다 — 값이 두 벌이 되면 반드시 어긋난다.

export const GESTURE = {
  startHoldSec: 3.0,          // 카메라 준비 화면: O 포즈 유지 시간 → 캘리브레이션 시작
  confirmHoldSec: 1.5,        // 종료 확인/게임오버 화면: O(계속)·X(종료) 유지 시간
  tutorialHoldSec: 1.0,       // 튜토리얼 화면: O=건너뛰기, X=이전 화면으로
  duringPlayExitHoldSec: 1.5, // 게임 진행 중: X 유지 시 종료 확인창 열기

  // O: 손목이 코보다 이만큼(bodyHeight 비율) 위에 있어야 "든" 것으로 인정
  armsUpMargin: 0.12,
  // O: 두 손목 사이 거리가 이 이내(bodyHeight 비율)면 동그라미로 인정
  closeMargin: 0.28,
  // X: 두 손목이 최소 이만큼(어깨너비 비율)은 벌어져야 교차로 인정(낮을수록 쉬움).
  //    발목 대신 어깨너비 기준 — 플레이 중에도 안정적으로 인식되도록.
  crossSeparation: 0.15,
}

/**
 * 기본 동작 문턱 — 점프 · 앉기 · 좌우 · 만세.
 *
 * 위쪽 넷은 러너의 `config.js`에 있던 값을 그대로 옮긴 것이다. **현장에서 다듬어졌다.**
 * 러너도 이제 여기서 가져다 쓴다 — 값이 두 벌이 되면 반드시 어긋난다(이 파일이 생긴 이유).
 *
 * 전부 `bodyHeight`(코~발목) 비율이라 아이 키와 카메라 거리를 타지 않는다.
 */
export const MOVES = {
  // ── 러너에서 검증된 값 (건드리지 말 것) ──
  laneThreshold: 0.12,    // 골반 x가 중앙에서 이만큼 벗어나면 좌/우
  laneHysteresis: 0.03,   // 돌아왔다고 보는 여유 — 없으면 경계에서 덜덜 떨린다
  jumpRise: 0.07,         // 골반 y 상승량
  jumpVelocity: 0.25,     // 상승 속도(bodyHeight/초). 높이만 보면 까치발도 잡힌다
  jumpCooldown: 0.6,      // 같은 점프를 두 번 세지 않는다
  emaAlpha: 0.35,         // 랜드마크 스무딩
  calibrationSec: 1.5,    // 러너 전용 — 새 감지기는 캘리브레이션을 하지 않는다

  // ── 캘리브레이션 없는 감지기(`detectors/moves.js`)가 쓰는 값 ──
  squatDrop: 0.14,        // 골반 y 하강량 (러너의 duckDrop과 같은 값)
  returnRatio: 0.6,       // 이만큼 되돌아오면 "일어났다" — 러너의 duck 해제와 같은 비율
  armRaise: 0.06,         // 손목이 어깨보다 키의 6% 위 → 만세.
                          // 코 기준(제스처의 0.12)보다 낮다 — 여기선 빠른 반복이 목적이라
                          // 팔을 끝까지 안 올려도 세어야 리듬이 산다
  baseWindowSec: 2.0,     // 좌우 중앙을 찾는 창 (highKnees와 같은 값)
  standWindowSec: 3.0,    // "서 있을 때 골반 높이"를 찾는 창.
                          // 좌우보다 길다 — 앉기가 이 창의 절반을 넘으면 기준이 따라 내려간다
  squatToJumpLockSec: 0.35, // 앉았다 **일어나는 순간**은 골반이 빠르게 올라가 점프로도 보인다.
                          // 실제로 스쿼트 한 번이 점프로 세어졌다 — 직후 잠깐은 점프를 막는다
  hipFromAnkle: 0.45,     // 발목에서 골반까지 키의 45% — 해부학적 폴백.
                          // 시작하자마자 점프 중이면 그 높이가 기준이 되는 걸 막는다
  minBodyHeight: 0.15,    // 전신이 안 보이면 아무것도 하지 않는다
}

// 바꾸면 안 되는 규칙 (04 통합작업순서 §4-2)
//
// | 규칙                          | 근거                                        |
// |-------------------------------|---------------------------------------------|
// | O만 머리 위 요구, X는 미요구  | X를 머리 위로 제한하면 플레이 중 인식률 급락 |
// | X는 어깨·손목만으로 판정      | 전신 기준이면 달리기 중 발목 프레임 이탈     |
// | GestureHold 감소율 dt × 0.6   | 즉시 리셋하면 프레임 흔들림에 처음부터 다시  |
// | 임계값은 bodyHeight 비율      | 아이/어른·카메라 거리 무관                   |
