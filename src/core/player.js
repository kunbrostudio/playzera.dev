// 지금 플레이하는 아이가 누구인지 — 아직 계정이 없다.
//
// 기획안 2단계에서 아이 선택이 들어오면 이 함수 하나만 바꾸면 된다.
// 게임마다 이름을 물어보지 않는 이유는 두 가지다.
//   · 4~8세가 매번 이름을 입력하는 건 게임을 시작하기 전에 넘어야 할 벽이다.
//     (키보드가 없는 기기에서는 더 그렇다)
//   · 누가 했는지는 결국 계정이 정해야 한다. 매번 손으로 적은 이름은
//     오타 하나로 다른 사람이 되고, 운동 기록이 갈라진다.
//
// 그때까지는 브라우저 하나 = 한 명으로 본다. 웜업이 원래 쓰던 값을 그대로 쓴다 —
// game_results에 이미 이 값으로 쌓인 기록이 있어서 규약을 둘로 만들면 안 된다.
const LOCAL_PLAYER = 'local-default'

export function getCurrentPlayerName() {
  return LOCAL_PLAYER
}
