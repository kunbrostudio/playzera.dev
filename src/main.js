import './core/router.js'
import { flushResults } from './core/resultQueue.js'

// 지난번에 못 보낸 기록을 다시 보낸다. 와이파이가 끊긴 채로 놀았던 판이다 —
// 그대로 두면 **아이가 움직인 사실이 사라진다.**
// 게임 화면을 막지 않도록 결과를 기다리지 않는다.
flushResults().catch(() => { /* 다음 기회에 */ })

// **BGM을 여기서 틀지 않는다.**
//
// 예전에는 앱이 뜨자마자 bgmPlay()를 불렀다. 허브가 렌더될 때 stop()으로 껐지만,
// 그건 순서에 기대는 방식이라 순서가 어긋나면 그대로 새어 나왔다 —
// 실제로 허브에서 음악이 계속 나왔다.
//
// 소리는 **켜는 쪽이 책임진다.** 게임팩이 자기 화면에서 켜고, 나갈 때 꺼진다.
// 게임이 아닌 화면(허브·시작·내 친구·마이페이지)은 라우터가 조용히 만든다.
