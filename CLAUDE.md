# 플레이 제라 — 작업 안내

카메라로 아이(4~8세)의 몸 동작을 인식해 노는 웹 게임 허브. 운동 데이터가 쌓이는 것이 이 서비스의 존재 이유다.

## 먼저 읽을 것

**`docs/00_지금상태.md`** — 한 페이지. **세션을 시작하면 이것부터 읽는다.**
지금 어디까지 왔고 · 다음 할 일 하나가 무엇이고 · 자주 밟는 것이 무엇인지가 있다.

그다음은 필요할 때만 연다.

**`docs/04_playzera_통합작업순서.md`** 하나면 지금까지의 경위를 다 알 수 있다. 진행 표 · 무엇을 왜 그렇게 정했는지 · 겪은 버그와 원인이 전부 여기 있다. **작업 로그이자 정본이다.**

나머지는 참조용이다.

| 문서 | 언제 본다 |
|---|---|
| `docs/01_playzera_기획안_v4.md` | "왜 이 서비스인가", 실행 환경, 조작 체계, 로드맵 (**현재 상태 기준**) |
| `docs/02_playzera_시스템설계_v3.md` | 포즈 엔진·손 포인터 설계 근거 |
| `docs/03_playzera_화면설계_v3.md` | 홈 레이아웃, 머무르기 시간 규칙 |
| `docs/07_playzera_운동체계.md` | **무엇을 운동으로 세는가** — 동작·카메라 한계·품질 문턱 |
| `docs/08_playzera_러너_에셋규격.md` | 러너 테마 에셋 발주서 — 파일명·크기·바닥 기준점 |
| `docs/09_팝팝클리커_기획.md` | 클리커 즉시 반응. 판 흐름·수치·즉각반응 규칙 |
| `docs/10_쥬라기런3D_기획.md` | **다음 게임** — three.js 러너. 왜 곡률을 눈에만 주는지 · 성능 예산 |
| `docs/11_playzera_다음단계.md` | **무엇을 어떤 순서로 하나** — 남은 일의 우선순위와 근거 |
| `docs/STEP9_아이폰검증.md` | 실기기 검증 체크리스트 |

## 지금 구조

```
src/core/
  router.js       해시 라우팅 + onLeave(정리 훅)
  handSession.js  손 컨트롤 세션 (앱 수명 동안 하나)
  pointer.js      손목 커서 + 머무르기
  catalog.js      홈의 페이지·배지·카테고리 규칙 (순수 함수)
  recent.js       이어서 하기 (localStorage)
  player.js       현재 아이 — 계정 전 임시
  handControl.js  손 컨트롤 켜기 + 카메라 오류 문구 (화면들이 같이 쓴다)
  uiAssets.js     앱 전체 공용 UI 그림 경로의 정본 (`/assets/ui/`)
  icons.js        Lucide 모양 인라인 SVG. 이모지 대신 쓴다 (Obra 킷과 같은 세트)
  tutorialSeen.js 게임별 "튜토리얼 봤나" 기억
  confetti.js     꽃가루 — 축하하는 순간의 공용 연출 (캔버스 하나, 그림 파일 없음)
  readyScreen.js  카메라 준비 화면 (게임들이 같이 쓴다)
  gameShell.js    게임 화면 공용 뼈대 — 안내·카메라·결과·기록
  resultQueue.js  서버 저장 + 실패 시 큐 (게임팩이 직접 부르지 않는다)
  pose/
    poseEngine.js tasks-vision 래퍼. 카메라 스트림을 참조 카운팅으로 공유
    gesture.js    O/X 판정
    tuning.js     제스처 튜닝값 (현장 검증됨 — 함부로 바꾸지 말 것)
    requirements.js 게임별 요구 관절 — 동작에 쓰는 점(move)과 몸 크기 재는 점(scale)을
                  나눠 적는다. **scale은 없앨 수 있는 요구다** (미터 좌표가 오면)
    pipOverlay.js 스켈레톤·3분할 오버레이
    detectors/    동작 감지기 — moves(점프·앉기·좌우·만세) · highKnees · balance
                  **캘리브레이션을 하지 않는다** — 롤링 기준선으로 매 순간 다시 읽는다
    poseMatch.js  자세 채점 — 관절 각도 + 몸 비율 (웜업·요가가 같이 쓴다)
    poses.js      요가 자세 사전 (서서 하는 것만)
src/progress/     성장 — 레벨·배지·조건·저장. **화면은 state.js만 거친다**
  exercises.js    운동 사전 — 동작·단위·EXP·품질 문턱. **지표 이름의 정본**
  level.js        운동량 → EXP → 레벨 (점수는 연료가 아니다)
  badges.js       배지 목록 = 데이터. 아이콘은 모티프 라이브러리를 돌려 쓴다
  conditions.js   조건 네 종류 (total · daysInWindow · variety · event)
  state.js        localStorage 하나 — 계정이 생기면 여기만 바꾼다
  rewardView.js   게임 결과에 붙는 배지·레벨 게이지
  report.js       부모 화면용 집계 — 하루 목표 30분·주간·게임별 (순수 함수)
  buddyView.js    버디 그림·별·게이지 (/buddy와 /me가 같이 쓴다)
src/buddies/      버디 registry + 종류별 manifest.json (단계 egg·hatch·grow·hero)
src/profiles/     아바타 registry
src/pages/        home · intro · tutorial · play · start · buddy · me
                  앞의 넷은 디스패처, 뒤의 셋은 성장 화면이다
src/games/
  registry.js     게임팩 목록. 게임을 추가할 때 손대는 유일한 파일
  poop-dodge/     intro.js · tutorial.js · play.js · game.js
                  lanes.js — 이 판을 3칸으로 할지 5칸으로 할지 (순수 함수)
  fire-rescue/    play.js · game.js — 제자리 달리기로 불 끄기 (유산소)
  stone-bridge/   play.js · game.js — 한 발로 버텨 돌다리 건너기 (균형)
  pop-clicker/    play.js · game.js · assets.js · sound.js — 클리커를 몸으로 누른다
  runner/         **러너 엔진** — 달리며 장애물 피하기. 게임을 모른다
    ui/hud.js     인게임 HUD — **3D 러너와 같이 쓴다.** 모양(CSS)도 여기 있다
    ui/cues.js    방향 힌트·카운트다운·레벨 완료·**판정 글자(Great!/Miss)**.
                  그림은 `_shared/` 상수, 판정 글자는 글자다 (게임마다 안 늘어난다)
    ui/touchPad.js 화면 버튼 ◀▼▶·A·S·D. **배선까지** 공용이다
    ui/systemBar.js **플레이 제라 공통 게임 UI의 정본** — Home·햄버거·나가기·종료 확인.
                  게임을 추가할 때 그대로 받아 쓴다. 모양을 따로 짜지 않는다
    audio.js      BGM·SFX. `initAudio({base, bgm})` — 3D도 같이 쓴다
    theme.js      테마 홀더. 꽂기 전에 읽으면 즉시 터진다
                  `ui/…`=앱 공용 · `_shared/…`=러너 공용 · 그 외=테마 폴더
    index.js      makeRunnerPlay(theme)
    game/ · input/ · screens.js · main.js · stats.js · legacy-shell.js
  runner-space/   manifest + theme.json + play.js(3줄) — 우주 웜업
  runner-jungle/  같은 모양 — 정글   (그림은 도형 플레이스홀더 상태)
  runner-jurassic/같은 모양 — 쥬라기 (그림은 도형 플레이스홀더 상태)
  runner3d/       **3D 러너 엔진** (three.js). 곡률은 눈에만 준다 — `curve.js`
                  `portal.js` 결승 포털 — 문(반투명 셰이더)·불. 인스턴싱 통 밖이다
                  `backdrop.js` 원경은 곡률을 안 받는 원통 띠 — 테마 그림을 합쳐 감는다
                  `models.js` GLB에서 **지오메트리만** 꺼낸다 (재질은 게임 것)
                  `props.js`  PropRow(서 있는 것) · DinoRow(부위를 흔드는 것)
                  `screens.js` 타이틀·튜토리얼 (라우트가 아니라 플레이 안이다)
                  규칙은 `runner/game/course.js`를 import해서 쓴다 (복사 안 함)
  jurassic-run-3d/manifest + play.js — `makeRunner3dPlay(manifest)`. 엔진은 이름을 모른다
  placeholders.js 개발용 더미
tools/blender/    **3D 에셋을 만드는 스크립트.** 그림이 아니라 만드는 방법이 정본이다
                  `pzblender.py` 공용 · `make_*.py` 에셋별 · `preview/` 렌더
test/             Vitest — 순수 로직만
```

**게임팩 규격** — `manifest.json`(제목·썸네일·`metrics`) + `game.js`(규칙만, DOM을 모른다)
+ `play.js`(화면, `core/gameShell.js`를 쓴다) + registry 한 줄.
`manifest.metrics`는 **이 게임이 만드는 운동 지표의 선언**이고, 셸의 기록기가 그 목록으로
스냅샷을 거른다. 사전에 없는 이름을 적으면 테스트가 잡는다.

**화면은 게임팩이 소유한다.** `src/pages/`의 넷은 내용이 없다 — `?id=`로 registry를
찾아 게임팩이 등록한 로더를 부를 뿐이다. 라우터도 허브도 게임 이름을 모른다.

```
/              허브
/intro?id=     게임팩 스플래시   (없으면 건너뜀)
/tutorial?id=  게임팩 튜토리얼   (없으면 건너뜀)
/play?id=      게임팩 플레이

/start         첫 실행 — 프로필·알 고르기 (한 번)
/buddy         내 친구 — 버디·배지·꾸미기 (아이 화면)
/me            마이페이지 (부모 화면) — 손 커서를 붙이지 않는다
```

게임팩이 등록하는 화면 모듈은 **default export가 렌더 함수**여야 한다.

## 규칙

- **카메라는 앱 전체에서 하나다.** `poseEngineCore.acquire()`/`release()`로 빌려 쓴다. 화면마다 열고 닫으면 커서가 끊기고 `NotReadableError`가 난다.
- **좌표는 거울 좌표 하나다.** 엔진이 `1 - x`를 한 번만 한다. 구독자는 뒤집지 않는다.
- **정리는 라우터의 `onLeave`로 한다.** 페이지가 `hashchange`를 직접 들으면 이미 `#app`이 비워진 뒤에 정리가 돈다.
- **`pose/tuning.js`의 값은 현장에서 다듬어진 것이다.** 구조는 바꿔도 수치는 근거 없이 건드리지 않는다.
- 카드 클릭은 **선택**이고 실행은 히어로의 시작 버튼 하나뿐이다.
- **BGM은 게임 안에서만 난다.** 켜는 건 게임팩, 끄는 건 라우터(`GAME_ROUTES`)다. 화면이 각자 끄면 켜는 쪽과 순서가 엉켜 소리가 샌다 — 실제로 허브에서 음악이 계속 났다.
- 개발용 더미 게임은 `import.meta.env.DEV`에서만 목록에 들어간다.
- **늘어나는 것은 데이터로 둔다.** 게임·버디·배지·아이콘·프로필 전부 registry 한 줄 + 그림 한 장으로 추가된다. 화면 코드는 개수를 모른다.
- **기록 키(`game_id`)를 코드에 박지 않는다.** 러너 엔진을 여러 게임이 공유하므로 테마가 갖는다. 박아두면 새 게임의 운동 데이터가 옛 게임 기록에 합쳐지고, 섞인 것은 되돌릴 수 없다.
- **생성 도구는 사람이 만든 것을 덮지 않는다.** 도구가 만든 파일 목록(`.placeholders`)을 남기고 그것만 다시 만든다. 실제로 `--force`가 완성된 우주 에셋 74장을 도형으로 덮었다 — git 덕에 살렸다.
- **그림 파일 이름을 id에서 계산하지 않는다.** 이름은 디자인 쪽에서 온다 — registry가 파일명을 데이터로 들고 코드가 따라간다.
- **기록 저장은 셸이 한다.** 게임팩이 직접 `saveResult`를 부르지 않는다 — 붙이는 걸 잊으면 그 게임의 운동 데이터가 통째로 사라진다(실제로 그랬다).
- **EXP는 몸을 움직여야 오르는 값에서만 나온다.** 점수·`dodgeCount`는 가만히 서 있어도 오른다.
- **지표를 코드 여기저기에 문자열로 적지 않는다.** 동작은 `progress/exercises.js` 한 줄로 추가된다 — EXP 가중치도 마이페이지 칸도 거기서 나온다.
- **횟수만 세지 않는다.** 각 동작에 품질 문턱이 있고 감지기가 그걸 지킨다. 대충 흔들어도 오르는 값은 운동 데이터가 아니다.
- **소프트웨어 줌아웃은 없다.** 렌즈가 안 담은 것은 어디에도 없다 — `getCapabilities().zoom`도 대개 줌인만 준다. 담기는 범위를 늘리는 길은 둘뿐이다: **종횡비를 세로로**(세로 화각 1.78배) · **요구 관절을 줄이기**(전신 대신 그 게임이 진짜 쓰는 점). 둘 다 계산이라 `#/labcam`에서 재고 나서 고친다.
- **캘리브레이션을 시키지 않는다.** "3초간 가만히"는 지겹고, 저장해 두면 앉은 채로 잡힌 기준이나 다른 아이의 몸이 조용히 남는다. 문턱이 전부 `bodyHeight` 비율이라 롤링 기준선으로 매 순간 다시 읽으면 된다.
- **감지기는 시간을 밖에서 받는다.** 안에서 `performance.now()`를 읽으면 합성 프레임으로 테스트할 수 없다.
- **`visibility`가 낮은 프레임은 버린다.** MediaPipe는 화면 밖 관절의 좌표를 지어낸다 — 앉아 있는 사람에게서 걸음이 세어졌다.
- 아이 화면은 전부 손 커서로 되어야 하고(`data-pz-hit`), 부모 화면(`/me`)에는 붙이지 않는다.
- **3D 에셋은 손이 아니라 코드로 만든다.** `tools/blender/*.py`가 정본이다 — 색·크기·개수가 한 줄이고, 저장소에 남아 언제든 다시 뽑힌다. 원점은 발밑, 밝기는 정점 색에 굽고(실시간 조명 0개), 부품은 하나로 합친다(안 합치면 인스턴싱해도 부품 수만큼 그린다). 장애물 치수는 `obstacles3d.js`의 `KINDS`에서 스크립트로 간다 — **반대가 아니다.** 뛰어넘을 높이·숙일 틈·레인을 막는 폭은 판정의 일부다.
- **공룡 같은 움직이는 것은 뼈대를 안 쓴다.** `InstancedMesh`가 스킨드 메시를 못 써서 마리 수만큼 draw call이 든다. 몸통·머리·꼬리를 따로 내보내 각각 인스턴싱하고(`DinoRow`) 행렬로 흔든다 — 부위의 원점이 **붙는 자리**에 있어야 축이 맞는다. 마리마다 위상·박자를 흩뜨리지 않으면 열 마리가 한 몸처럼 고개를 젓는다.
- **길에는 턱이 있어야 길이다.** 트랙을 바닥에 그려 놓기만 하면 잔디에 **인쇄된 무늬**로 보인다. 3D에서는 높이 있는 연석을 세웠고, 그때 `BoxGeometry`를 쓰면 안 된다 — three의 상자는 면마다 uv 축이 달라(옆면 `v`=높이, 윗면 `v`=길이) 흘리면 윗면만 앞으로 오고 옆면은 위아래로 미끄러진다. 단면을 밀어 만든다(`extrudeStrips`).
- **오브젝트는 바닥에 붙어 있어야 한다.** 그림자·받침 없이 스프라이트만 그리면 아무리 좋은 에셋도 떠 있는 스티커로 보인다. 웜업 배경이 실제로 그랬다 — 고친 것은 에셋이 아니라 **놓인 자리**였다(`docs/04` STEP 12).
- **바닥에 놓이는 것은 테마, 화면에 뜨는 것은 공용.** 장애물·관문·출발선·배경은 테마가 갖고, 카운트다운·레벨 완료·시상대·자세 실루엣·방향 힌트는 `_shared/` 한 벌이다. 장애물까지 공용으로 만들어 봤는데 우주의 네온 큐브가 정글 흙바닥 위에서 그 세계의 물건으로 안 보였다 — 규칙을 알려주는 건 모양이고, 색과 재질은 세계를 따라간다.
- **주인공은 테마가 아니다.** 캐릭터는 `_shared/char/<skin>/`에 프로필별로 한 벌씩이고 러너 테마 전부가 같이 쓴다. 복사해 두면 한 게임만 옛 그림이 남고, 아이는 게임을 옮길 때마다 자기 캐릭터가 바뀌는 걸 본다.
- **캐릭터는 가입 성별이 아니라 아이가 고른 프로필(`state.profile`)로 갈린다.** 가입은 부모가 하고 캐릭터는 아이가 논다. 가입 없이도 게임이 돌아가므로 성별에 묶으면 비회원은 자기가 아닌 캐릭터를 받는다. 정본은 `src/core/playerSkin.js`.
- **그림을 상자에 맞춰 늘이지 않는다.** 하늘은 덮기(cover), 능선은 중앙 한 장 + 좌우 이어붙이기다. 늘이면 구름이 눌리고, 랜드마크가 있는 능선을 타일링하면 게이트가 서너 개로 늘어난다.
- **원근 요소는 화면 밖까지 그린다(`P_OVER`).** y와 반폭이 둘 다 p에 선형이라 모양은 그대로다. `p=1`에서 끊으면 연석 윗면이 lift만큼 위에서 끝나 근경 모서리에 노치가 생긴다 — 세 게임에 다 있었다.
- **줄은 자리의 기준이지 자리가 아니다.** 좌우 프롭은 물건마다 옆·속도·앞뒤를 흩뜨린다. 안 하면 가로수길이 된다. 스폰 간격도 흔들어야 한다 — 박자가 일정하면 흩어져도 기계적이다.
- **자리는 한 곳에서 관리한다**(`runner3d/layout.js`). 줄마다 따로 고르게 깔면 줄끼리는 서로를 모른다 — 브라키오 안에 티라노가 박혔다. 겹칠 짝은 줄 수의 제곱으로 는다. 되돌리기가 있으므로 z는 **고리처럼** 재고(z=-1과 z=-139는 이웃이다), **큰 것을 먼저 놓는다** — 작은 것은 큰 것 사이에 끼지만 반대는 안 된다. 공룡은 GLB를 받은 뒤에 만들어져 언제나 마지막이라 자리를 미리 예약해 둔다.
- **배경을 채우는 일은 가만히 서 있는 것들이 한다.** 공룡을 늘려 채웠더니 어지러웠다 — 움직이는 것이 많으면 눈이 쉴 데가 없고, 아이가 봐야 할 곳은 트랙이다. 큰 것은 두 마리만 보여도 무리로 읽힌다.
- **캐릭터는 3/4 각으로 세운다.** 트랙 옆면(±π/2)을 보게 하면 옆모습만 보이는데 얼굴은 앞에 있다. 정면으로 완전히 돌리면 종이 인형처럼 납작해진다. 나는 것은 다르다 — 세계가 아이 쪽으로 흐르므로 **θ≈0이 날아오는 방향**이다.
- **삼각형을 깎느냐 파일을 키우느냐는 거짓 양자택일이다.** AI 고품질 모델(25만)을 1만까지 깎으면 UV가 뭉개져 잎사귀가 진흙이 된다. Draco로 압축하면 둘 다 안 줄여도 된다 — 2.5배 촘촘한 메시가 절반 크기다. 다만 **내보내기와 로더는 한 몸이다**: 한쪽만 켜면 모델이 안 뜨는데 화면은 도형으로 조용히 돌아가 콘솔을 안 보면 모른다.
- **깎기 전에 붙인다** — "깨져 보인다"의 진짜 원인이었다. AI 메시는 삼각형이 **정점을 공유하지 않은 채로** 온다(190만 삼각형에 비매니폴드 모서리 35만). collapse는 경계를 못 접으니 접을 수 있는 안쪽만 무너뜨리고 갈라진 자리를 파편으로 남긴다. `remove_doubles(3e-4)` 한 줄이면 비매니폴드가 13만 → 177이 되고 1만2천 삼각형에서도 매끈하다.
- **막혔을 때 예산부터 늘리지 않는다.** 깨짐을 못 고쳐 "안 깎는 수밖에 없다"며 에셋 예산을 7 → 11MB로 올렸다. 원인을 찾고 나니 3만 삼각형에 5.65MB로 더 깨끗했다. 예산을 올리고 싶어지면 그건 대개 **아직 원인을 모른다는 신호**다.
- **고치기 전에 잰다.** 깨짐의 원인을 Draco UV 양자화로 짐작하고 12→16비트로 올렸는데 화면이 **똑같았다**(파일만 18% 커졌다). 결정적 단서는 블렌더 **솔리드 뷰**였다 — 텍스처를 끄고도 파편이 보이면 UV도 압축도 아니고 기하다.
- **곡률을 바꾸면 안개도 따라간다.** 접힘선이 √(h/k)라 R을 600 → 900으로 올리면 85 → 104로 밀려난다. 안개를 그대로 두면 접힘선 앞부터 잔디가 바래 원경이 아니라 자국으로 보인다.
- **앞뒤가 있는 물건은 아무렇게나 안 돌린다.** 화석 바위는 한 면에만 뼈가 새겨져 있어 랜덤 회전에서 절반이 갈색 덩어리로 보였다. 그런 프롭은 ±0.5rad만 흔든다(`faceTrack`).
- **AI 생성물은 부스러기를 달고 온다.** 5~6면짜리 조각이 허공에 떠 있다. 면 수로는 못 거른다 — 잎사귀·돌이 각각 떨어진 키트배시라 정상 부품도 그만하다. **크기**로 거른다(전체의 2% 미만). 단, **붙인 뒤에** — 안 붙인 메시는 삼각형 하나하나가 다 섬이다.
- **눌러 늘이지 않는다.** 원본 비율과 규칙이 다르면 기둥은 평행이동하고 가운데만 늘린다(`_fit_gate`·`_widen`). 통째로 스케일하면 무늬가 늘어져 그것도 "깨져 보인다"가 된다.
- **원경에 곡률을 주지 않는다.** 3D에서 능선 판에 곡률을 먹였더니 `k·z²`만큼(118유닛에서 11유닛) 통째로 내려앉아 지평선 밑으로 파묻혔다 — 화면에는 더 높이 띄워 둔 화산만 하늘의 스티커로 남았다. 멀리 있는 것은 **지평선에 붙어 있는 것**이고, 지평선은 세계가 휘어도 그 자리다. 붙일 자리는 바닥이 **접히는 각** `tan θ = 2√(k·h)`다 — 휜 바닥은 지평선까지 가지 않고 `√(h/k)`에서 끝난다.
- **하늘색을 바닥에 얹지 않는다.** 광택 빛줄기·헤이즈·깊이 안개 셋 다 잔디를 허옇게 띄웠다. 초록 위의 하늘색은 원경이 아니라 바랜 자국으로 보인다.
- **칸 판정에는 히스테리시스가 있어야 한다.** 등분해 자르기만 하면 경계에 선 아이가 미세 떨림으로 칸을 오가고, 그러면 가만히 서 있어도 `side_steps`가 오른다. 여유는 **칸 너비의 비율**이다 — 절대값으로 두면 칸이 좁아질 때 못 넘어간다.
- **한 판 안에서 규칙의 크기를 바꾸지 않는다.** 똥 피하기 칸 수(3·5)는 판 시작에 한 번 정한다. 중간에 바뀌면 아이가 혼란스럽고, 그 판의 운동량이 무엇을 뜻하는지 말할 수 없게 된다. 바꾼 축은 **기록에 함께 남긴다**(`extra_data.lanes`) — 섞인 것은 되돌릴 수 없다.
- **판정을 키보드에만 붙이지 않는다.** 3D 러너의 자세 판정이 키보드 핸들러에만 있었다 — **어른이 테스트할 때만 도는 길**이다. 아이가 카메라로 놀면 자세 팻말이 무조건 벽이었고 `pose_holds`는 언제나 0이었다. 몸으로 하는 입력이 본체고 키보드가 폴백이다.
- **지표는 이름이 맞는지가 아니라 실제로 쌓이는지를 본다.** `record('pose')`를 아무 데서도 안 부르는데 "사전에 있는 이름인가"·"manifest에 선언됐는가" 테스트는 둘 다 통과했다. 셀 줄 아는 종류를 읽어 **화면이 그걸 부르는지**까지 봐야 구멍이 잡힌다.
- **글자 키는 `e.code`로 읽는다.** 한글 입력기가 켜져 있으면 A·S·D가 `e.key`에 'ㅁ'·'ㄴ'·'ㅇ'로 온다. 방향키는 IME를 안 타므로 **자세 키만 조용히 안 먹고**, 화면만 보면 원인을 못 찾는다. 2D 러너에서 고쳐 놓고 3D에서 되풀이했다 — 테스트가 `src/` 전체를 훑는다.
- **캔버스 텍스처에는 `colorSpace = SRGBColorSpace`를 붙인다.** 안 붙이면 three가 그 픽셀을 선형으로 보고 출력에서 한 번 더 밝게 편다 — `#3f8a2f` 잔디가 `#87c475`로 떴다. 더 나쁜 것은 **한쪽만 붙였을 때**다: 바닥과 원경 띠에 같은 색을 적어 두고 테스트로 지켜도 화면에서는 색이 갈린다. 값이 같은데 화면이 다르면 색공간부터 본다.
- **화면을 떠나는 길이 둘이면 결과도 둘이어야 한다.** 한쪽만 `resolve`하고 다른 쪽을 콜백으로 빼면 그 길에서 `await`가 안 풀려 흐름이 통째로 멈춘다 — 화면은 비고 그 아래 직전 게임의 배경이 드러난다. `showReadyScreen`처럼 `'start'|'hub'` 같은 결과를 돌려준다.
- **정리(`onLeave`)는 화면을 그리기 전에 건다.** 라우터는 정리 함수를 **하나만** 들고 있고, 화면 중간에서 나가는 길이 여럿이다. 아래쪽에서 걸면 준비 화면에서 뒤로 나가는 것 같은 길에서는 아예 안 걸린다 — 실제로 그래서 BGM이 허브까지 따라갔다. 목록에 쌓고 하나가 훑는다.
- **머무르기 타겟을 세로로 인접하게 두지 않는다.** 1.2초 겨누는 동안 손이 조금만 움직이면 옆 버튼이 눌린다. 하단 4칸 바도, 허브 버디 자리도 이걸로 한 번씩 옮겼다.
- **공통 UI는 한 벌이다 — 이것이 브랜드다.** ★ Home·햄버거 메뉴·나가기·종료 확인은 `runner/ui/systemBar.js` 하나가 갖는다. 게임마다 컨셉과 규칙이 달라도 **나가는 법·소리 끄는 법·홈으로 가는 법**이 같아야 "플레이 제라 안에서 놀고 있다"가 이어진다. 다르면 그건 게임 모음이지 플랫폼이 아니고, 아이는 게임을 옮길 때마다 조작을 다시 배운다. 새 게임은 이 모듈을 **받아 쓴다** — 모양을 따로 짜지 않는다(`test/brandUi.test.js`가 막는다).
- **한 벌이어야 할 것을 두 벌로 만들면 CSS가 한쪽에만 걸린다.** 3D 타이틀이 자기 메뉴를 `#r3-menu-panel`로 그렸는데 닫는 규칙은 공용 `#pz-menu-panel`에만 있었다 — 아무도 열지 않았는데 아이콘 셋이 화면에 그대로 나와 있었다. 마크업을 베끼면 **모양은 안 따라온다.**
- **나가기는 한 단계씩 뒤로 간다.** 나가기 → 그 게임의 처음, 확인창의 "Home으로" → 허브. 한 번에 허브로 보내면 한 번 더 하려던 아이가 게임 밖으로 튕기고, 다시 들어오려면 허브에서 카드를 찾아야 한다.
- **나가는 버튼의 이름은 가는 곳이다.** "그만하기"·"게임 목록"은 무엇이 그만되는지·어디로 가는지 아이도 부모도 모른다. 허브의 이름은 **Home** 하나다.
- **확인창의 버튼은 세로로 쌓는다.** 셋이 가로로 서면 좁은 화면에서 줄이 넘치고, 머무르기 커서로 가운데를 겨누기 어렵다.
- **하나뿐이고 살아 움직이는 것은 인스턴싱 통에 안 넣는다.** `InstancedMesh`는 인스턴스마다 다른 재질·다른 시간을 못 준다. 결승 포털은 문이 반짝이고 탑에 불이 붙어야 해서 `runner3d/portal.js`가 따로 갖는다 — 대신 씬에 **한 번만** 붙이고 z만 옮긴다.
- **빛으로 그린 것은 깊이를 안 쓴다.** 반투명 문에 `depthWrite`를 켜 두면 그 판이 깊이 버퍼를 채워 **뒤에 뚫어 놓은 통로가 지워진다** — 문이 아니라 노란 벽이 된다. 가산 합성 + `depthWrite: false` + `renderOrder`가 한 벌이다.
- **AI 키트배시는 부품마다 텍스처를 들고 온다.** 결승 포털은 부품이 59개였다 — 그대로 합치면 재질이 59개라 draw call도 59다(예산은 20). 굽지 않고 **텍스처를 타일로 늘어놓고 UV를 옮긴다**(`_atlas`). UV가 전부 0…1 안에 있으면 그게 가능하고, 결과가 매번 똑같다. 칸 크기는 삼각형 수로 준다 — 큰 부품이 곧 크게 보이는 부품이다.
- **모델을 자를 자리는 비율로 적는다.** 원본 치수로 적으면 AI가 다시 뽑을 때마다 숫자를 고쳐야 한다. 경계상자 대비 x(−1…1)·y(0=앞)·z(0=바닥)로 적으면 모델이 바뀌어도 같은 자리를 가리킨다.
- **자르기는 깎기 전이다.** 디시메이트가 끝난 뒤에 면 중심으로 지우면 삼각형이 이미 커서 구멍 테두리가 **톱니처럼** 남는다. 원본에서 자르면 매끄럽고, 이어지는 디시메이트는 경계 모서리를 못 접으니 그 테두리를 그대로 지킨다.
- **연출용 자리는 재서 옮겨 적는다.** 문·불의 위치를 눈대중으로 적었더니 불이 탑 바깥 허공에 떠 있었다(8.35 → 실측 7.49). 스크립트가 재서 돌려주고(`_take_door`) 게임이 그 숫자를 쓴다.
- **결승선에서 마지막 피하기를 시키지 않는다.** 문(5.3)이 트랙(15)보다 좁아 바깥 레인의 아이는 기둥에 박힌다. 문을 넓히려면 가운데를 늘려야 하는데 거기 해골이 있다. 규칙 대신 연출로 푼다 — 26유닛 앞부터 조용히 가운데 레인으로 모은다(`PORTAL_FUNNEL`). 판정은 어차피 통과다.
- **결승선은 마지막 장애물이 아니라 도착 지점이다.** 마지막 포즈 팻말 1.5초 뒤에 뒀더니 자세를 잡고 있던 아이가 팻말을 지나자마자 끝나 버렸다 — 알아차릴 새가 없다. 5초를 두고, **speed로 나누지 않는다**: 연출이라 레벨이 빨라져도 같은 시간이어야 하는데, 나누면 하필 결승선이 나오는 마지막 레벨에서 제일 짧아진다.
- **끝나는 순간은 문 앞이 아니라 문 뒤다.** 판정이 걸리는 자리에서 바로 끝내면 화면이 문 앞에서 툭 끊겨 통과가 아니라 막힌 그림이 된다. 조금 더 굴려서 문이 카메라 뒤로 넘어간 다음에 끝낸다(`PASS_MS`).
- **엔딩과 결과 화면은 따로다.** 결과는 점수·배지·버튼이 있는 **읽는 화면**이고, 엔딩은 읽을 것이 없는 화면이다. 한 화면에 얹으면 그림은 UI에 가리고 버튼은 그림에 묻힌다. 끝냈다는 감정이 제일 큰 순간(문을 지난 직후)에는 그림만 보이고, 읽을 것은 그다음에 온다. **못 깬 아이에게는 엔딩을 안 띄운다** — 축하가 아니라 놀림이 된다.
- **템플릿 문자열 안 주석에 백틱을 쓰지 않는다.** CSS·HTML을 백틱 문자열에 담는데 그 안 주석에서 파일 이름을 \`이렇게\` 감싸면 문자열이 거기서 끝나고 파일이 통째로 안 열린다. 여섯 번 겪었다 — `test/sourceParses.test.js`가 파일 이름을 대며 잡는다.
- **장애물마다 즉시 답을 준다.** 하트가 하나 줄어드는 것만으로는 4~8세가 "무엇 때문에 줄었나"를 못 잇는다. Great!/Miss는 화면 한가운데가 아니라 **방금 그 아이가 선 칸의 머리 위**에 뜬다 — 세 칸을 오가는 게임이라 자리가 곧 정보다. 실패에 **빨강을 안 쓴다**(분홍): 놓친 것은 실패가 아니라 다음 것이 온다는 뜻이고, 경고색은 아이를 굳게 만든다.
- **연출을 그림 파일로 늘리지 않는다.** Great!/Miss를 PNG로 만들면 테마 수만큼 는다. 획이 굵은 폰트에 `paint-order: stroke`로 테두리를 두르면 그림과 구별이 안 가고, 게임이 늘어도 파일은 그대로다.
- **아이가 방금 몸으로 해본 것을 글로 다시 읽히지 않는다.** 튜토리얼 뒤에 안내 화면(제목·"어떻게 하나"·"왜 좋은가")이 있었는데, 같은 말을 한 번 더 하면서 버튼을 한 번 더 누르게 했다. 몸으로 하는 게임에서 화면이 하나 늘면 그만큼 **안 움직이는 시간**이 는다.
- **뒤로는 한 단계씩 간다.** 튜토리얼 ─뒤로→ 카메라 준비 ─뒤로→ 타이틀 ─Home→ 허브. 그래서 앞 화면 고리가 둘이다(`title:` 라벨) — 하나로 두면 튜토리얼의 뒤로가 타이틀까지 튕겨 나간다. 뒤로 버튼은 **앞 화면과 같은 자리**(아래 줄)에 둔다.
- **앞뒤로 오가는 화면에 밖으로 나가는 문까지 두지 않는다.** 튜토리얼에 뒤로·건너뛰기·Home 셋을 뒀더니 고를 것이 셋이 됐다 — 4~8세에게 셋이면 하나는 잘못 눌린다. 나가는 길은 확인창 안에 모은다.
- **판을 시작도 안 했으면 기록하지 않는다.** 나가는 길마다 "지금까지의 운동을 저장"을 부르는데, 튜토리얼·안내에서 나가면 0짜리 기록이 남아 하루 30분 집계에 섞인다. 카운트다운이 끝난 뒤부터가 판이다.

## 확인 명령

```bash
npm run check   # ★ 이거 하나면 된다 — 테스트·빌드·맥용 rollup·에셋 용량을 다섯 줄로
npm run dev     # localhost:5173  ← **ken이 켠다.** Claude의 셸은 별도 리눅스라 여기 못 닿는다
npm test        # Vitest만 따로
npm run build   # 프로덕션 빌드만 따로
npm run world   # 러너 배경만 PNG로 뽑는다 (브라우저 없이)
                # `npm run world -- out.png 8.5 runner-jungle`
node tools/makePlaceholders.mjs src/games/runner-jungle/theme.json   # 도형 에셋 채우기
                # --force는 `.placeholders`에 적힌 것만 다시 만든다 (진짜 그림은 안 덮는다)
```

개발용 화면 (DEV에서만)
- `#/lab`    — 감지기가 웹캠으로 실제로 세는지 눈으로 본다
- `#/lab3d`  — 3D 러너 성능 게이트. 포즈를 켠 채로 FPS를 재고 곡률을 눈으로 고른다
- `#/labcam` — 카메라 화각 실측. 종횡비를 바꿔가며 **몸이 프레임을 얼마나 차지하나**를 잰다.
               "얼마나 물러서야 하나"의 근거가 여기서 나온다

브라우저 콘솔에서
- `pzResetRecent()` — 이어서 하기 기록을 지운다
- `pzResetProgress()` — 버디·배지·레벨을 지워 `/start`부터 다시 본다

## 지금 위치

`main` 브랜치. 게임 8개(똥 피하기·우주 웜업·정글·쥬라기·쥬라기 3D·불 끄기·돌다리·팝팝 클리커), 테스트 697건.
STEP 5(게임팩화)·10(성장)·11(운동 체계)·12(배경 개선)·13(러너 테마화)·14(팝팝 클리커)까지 들어갔고
**크롬에서만 확인됐다.**

**지금 막혀 있는 것은 실기기 검증이다.** 불의 성장률·달리기 케이던스 문턱·균형 EXP·
요가 자세 문턱은 전부 아이가 실제로 움직여 봐야 정해지는 값이라 어림으로 두었다.
`#/lab`에서 감지기와 요가 채점을 눈으로 볼 수 있다.

**정글·쥬라기는 도형 플레이스홀더 상태다.** 진짜 그림이 오면 같은 이름으로 덮으면 된다 —
발주서는 `docs/08`, 확인은 `npm test`(선언했는데 없는 파일을 잡는다).

남은 것: 팝팝 클리커 실기기 검증(라운드 68회·팔 문턱) · 정글·쥬라기 에셋 교체 · `actions.js`+`course.json`으로 규칙 데이터화(정글에
제자리 달리기·한 발 버티기·동물 요가) · 실기기 문턱 조정 · 부화 연출 ·
똥 피하기/러너를 공용 셸로 · STEP 9 마무리(Render 중단·`_archive` 정리).

## 일하는 방식

- 문서에 적힌 판단 근거를 먼저 읽는다. 이유가 적혀 있는 결정은 대개 이유가 있다.
- 코드를 고치면 `docs/04`의 해당 STEP에 **무엇을 왜 그렇게 했는지** 남긴다. 체크박스만 채우지 않는다.
- 브라우저에서만 확인되는 것(카메라·프레임·좌우 반전)은 추측하지 말고 확인을 요청한다.
- **고치기 전에 잰다.** 이 규칙으로 실제 이득을 본 게 여러 번이다 — Draco 양자화(재보니 화면이 똑같았다) · 에셋 예산(원인을 찾고 나니 예산이 필요 없었다) · 토큰(범인이 CLAUDE.md인 줄 알았는데 5%였다).
- **주제 하나에 세션 하나.** 토큰의 대부분은 맥락이 **오래 남아서** 나간다 — 한 번 들어온 것은 그 뒤 모든 턴에서 다시 읽힌다. 6일짜리 세션 하나가 1,706턴이었고, 200턴째 올린 스크린샷 한 장이 20만 토큰이었다. 일이 끝나면 `docs/00`을 갱신하고 끊는다.
- **말로 적은 규칙은 매 세션 돈을 내고, 그러고도 안 지켜질 수 있다. 테스트로 적은 규칙은 공짜이고 어길 수가 없다.** 새 규칙이 생기면 테스트로 옮길 수 있는지 먼저 본다(`brandUi` · `sourceParses` · `keyboardIme`가 그렇게 생겼다).

## Second Brain 연동

이 프로젝트의 Second Brain 경로는 `~/Documents/kunbro-brain` 이다.

**"Second Brain에 ingest해줘"** 요청을 받으면:

1. 방금 작업/대화 내용에서 중요한 결정, 실험 결과, 완료된 기능을 추출한다.
2. `~/Documents/kunbro-brain/wiki/projects/play-zera.md` 를 **먼저 읽는다.**
3. 관련 내용이면 그 파일 또는 `wiki/decisions/`, `wiki/experiments/`의 관련
   canonical page를 업데이트한다. 관련 페이지가 없으면 `templates/decision.md`
   또는 `templates/experiment.md` 형식을 따라 새로 만든다.
4. 원본을 보존해야 하면 `~/Documents/kunbro-brain/raw/projects/play-zera/`에 저장한다.
5. `~/Documents/kunbro-brain/LOG.md`에 변경 내역을 append한다.
6. **전체 vault를 읽지 않는다** — INDEX → 검색 → 관련 wiki → raw 순서로 관련 파일만 연다.
7. **추측해서 채우지 않는다** — 실제 대화/작업에서 나온 내용만 기록한다.
   모르는 필드는 비워 두고, 필요하면 묻는다.

`docs/04`와 역할이 다르다. `docs/04`는 **이 저장소의 작업 로그**(무엇을 왜 그렇게
고쳤나)이고, Second Brain은 **프로젝트를 가로지르는 지식**(전략 결정·실험 결과)이다.
같은 내용을 양쪽에 적지 않는다.
